# Emacs Configuration

This directory uses **literate programming** principles. Configuration is written
in `.org` files using Org-mode, with code blocks that tangle out to `.el` files
for Emacs to load.
